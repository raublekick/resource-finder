// import type { Resource } from '@/types/resource'

// export const useResources = () =>
//   useState<Resource[]>('resources', async () => {
//     const { data } = await useFetch<Resource[]>('/api/resources')
//     return data.value || []
//   })